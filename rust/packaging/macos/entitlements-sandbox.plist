<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Application identifier (must match provisioning profile) -->
    <key>com.apple.application-identifier</key>
    <string>T822AUM7XY.io.github.lobotomoe.revenant</string>
    <!-- App Sandbox (required for Mac App Store) -->
    <key>com.apple.security.app-sandbox</key>
    <true/>
    <!-- Network: outbound connections to CoSign server -->
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- File access: user-selected files via open/save dialogs -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
    <!-- No cs.allow-unsigned-executable-memory / cs.disable-library-validation:
         those were needed only by the Python client's bundled interpreter. The
         self-contained Rust binary needs neither, and omitting them keeps the
         hardened runtime intact -- disable-library-validation in particular is a
         common App Store review rejection. -->
</dict>
</plist>
